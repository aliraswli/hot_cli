name: feature
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ^0.1.1

vars:
  name:
    type: string
    description: Clean Architecture feature name
    default: new_feature
    prompt: What is the feature name?
